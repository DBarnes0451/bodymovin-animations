var animation = bodymovin.loadAnimation({
    container: document.getElementById('heart-anim'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'heart-animation.json'
  })

  var animation = bodymovin.loadAnimation({
    container: document.getElementById('star-anim'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'star-animation.json'
  })

  var animation = bodymovin.loadAnimation({
    container: document.getElementById('cog-anim'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'cog-animation.json'
  })

  var animation = bodymovin.loadAnimation({
    container: document.getElementById('mail-anim'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'mail-animation.json'
  })

  var animation = bodymovin.loadAnimation({
    container: document.getElementById('call-anim'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'call-animation.json'
  })

  var animation = bodymovin.loadAnimation({
    container: document.getElementById('share-anim'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'share-animation.json'
  })